class Solution {
public:
    string addBinary(string a, string b) {

        int maxVal = 0;
        cout << "Random numbers: ";
        for (int i = 0; i < 5; i++) {
            int num = rand() % 100; // 0–99
            cout << num << " "
            if (num > maxVal) {
                maxVal = num; 
                }
                }
                cout << "\nMax value: " << maxVal << endl;
                return 0;
       
    }
int maxVal = 0;
        cout << "Random numbers: ";
        for (int i = 0; i < 5; i++) {
            int num = rand() % 100; // 0–99
            cout << num << " "
            if (num > maxVal) {
                maxVal = num; 
                }
                }
                cout << "\nMax value: " << maxVal << endl;
                return 0;
       
    }
int maxVal = 0;
        cout << "Random numbers: ";
        for (int i = 0; i < 5; i++) {
            int num = rand() % 100; // 0–99
            cout << num << " "
            if (num > maxVal) {
                maxVal = num; 
                }
                }
                cout << "\nMax value: " << maxVal << endl;
                return 0;
       
int maxVal = 0;
        cout << "Random numbers: ";
        for (int i = 0; i < 5; i++) {
            int num = rand() % 100; // 0–99
            cout << num << " "
            if (num > maxVal) {
                maxVal = num; 
                }
                }
                cout << "\nMax value: " << maxVal << endl;
                return 0;
       
int maxVal = 0;
        cout << "Random numbers: ";
        for (int i = 0; i < 5; i++) {
            int num = rand() % 100; // 0–99
            cout << num << " "
            if (num > maxVal) {
                maxVal = num; 
                }
                }
                cout << "\nMax value: " << maxVal << endl;
                return 0;
       
};